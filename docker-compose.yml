services:
  db:
    image: postgres:17.7
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: postgres
      POSTGRES_DB: postgres
    ports: 
      - 5434:5432
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: {}
